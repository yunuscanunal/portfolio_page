spring.application.name=portfolio-backend

# --- Veritabanı Ayarları (DataConfig.java tarafından yönetiliyor) ---
# spring.datasource.url=... (SİLİNDİ veya YORUMA ALINDI)
# spring.datasource.username=... (SİLİNDİ veya YORUMA ALINDI)
# spring.datasource.password=... (SİLİNDİ veya YORUMA ALINDI)

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT ve Diğer Ayarlar
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=86400000

admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:admin123}

server.port=${PORT:8080}
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
